[project]
name = "dl-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aarush Rawat", email = "rawataarush2005@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "pydantic>=2.13.5",
    "rich>=15.0.0",
    "scikit-learn>=1.9.1",
    "tensorflow>=2.21.0",
]

[project.scripts]
dl-project = "dl_project:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
